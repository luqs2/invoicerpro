services:
  - type: static_site
    name: invoicerpro-staging
    buildCommand: npm install && npm run build
    staticDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
